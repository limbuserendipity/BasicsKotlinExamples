package org.example.oop.case1_ClassAndObjects.tasks

import kotlin.random.Random

/*

    Сделать программу,
    которая генерирует «карту»,
    где спрятаны разные предметы (сокровища, монстры, ловушки) в случайных местах.

    Пример карты:

    T T M T X . . T M X
    M X . X X T . T T T
    M . T . . . T M X T
    M . X M X T X X M .
    X T M M T X M T T M

    class MapCell(
            x: Int, y: Int — координаты клетки (или строка/столбец).
            content: String — что в клетке (пусто, сокровище, монстр, ловушка).
    ){
    }
    class TreasureMap(
            Размер карты (например, width, height).
            Список (или двумерный массив) MapCell.
    ){
        Метод generateMap() — каждую клетку «заполнить» случайной вещью:
    }

    val contents = listOf("Пусто", "Сокровище", "Монстр", "Ловушка")
    В main():
    Создать объект TreasureMap с заданной шириной и высотой.
    Вызвать generateMap().
    Отобразить карту на экране
    (например, выводить символы: . для пустого, T для сокровища, M для монстра, X для ловушки).

 */

fun main() {

}